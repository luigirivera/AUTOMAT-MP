package driver;

import view.Frame;
import model.*;
import automat_new.*;

public class Driver {
	public static void main(String[] args)
	{
		new Frame(new Model());
		Automaton a = new Automaton();
		
	}
}
